name: Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on:
      group: Default
      labels: self-hosted

    steps:
      - name: Test runner
        run: |
          echo "================================"
          echo "RUNNER IS WORKING"
          echo "================================"
          echo "Hostname:"
          hostname
          echo "User:"
          whoami
          echo "Working directory:"
          pwd
          echo "Docker:"
          docker --version
